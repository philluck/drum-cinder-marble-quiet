import { createFileRoute } from "@tanstack/react-router";
import { LegalLayout } from "@/components/legal-layout";
import { SITE } from "@/lib/site";

export const Route = createFileRoute("/cookies")({
  component: CookiesPage,
  head: () => ({
    meta: [{ title: `Файлы cookie — ${SITE.name}` }],
  }),
});

function CookiesPage() {
  return (
    <LegalLayout title="Файлы cookie и локальное хранилище">
      <p>
        Сайт {SITE.domain} не использует рекламные и аналитические cookie третьих
        лиц. В браузере сохраняются только технические данные, без которых сервис
        заказа не работает.
      </p>
      <h2>Что сохраняем</h2>
      <p>
        <strong className="text-fg">tupih-cart</strong> — состав заказа (позиции и
        количество), чтобы список не пропадал при обновлении страницы.
      </p>
      <p>
        <strong className="text-fg">tupih-cookie-consent</strong> — отметка, что вы
        видели это уведомление и не нужно показывать его снова.
      </p>
      <h2>Срок</h2>
      <p>
        Записи хранятся на вашем устройстве бессрочно, пока вы не очистите данные
        сайта в настройках браузера или не нажмёте «Очистить» в заказе.
      </p>
      <h2>Отказ</h2>
      <p>
        Вы можете запретить запись в настройках браузера. Тогда корзина не будет
        сохраняться между визитами, оформить заказ через форму всё равно можно в
        рамках одного сеанса.
      </p>
      <p>
        Правовая основа: необходимость для оказания услуги по вашей инициативе и
        ст. 6 152-ФЗ в части технических данных, а также требование информировать
        пользователя о cookie в соответствии с практикой Роскомнадзора.
      </p>
    </LegalLayout>
  );
}
